{
  "name": "myepitech-stats",
  "private": true,
  "version": "1.0.0",
  "description": "Timeline view of your Epitech modules and projects, scraped from my.epitech.eu, for planning your credit strategy.",
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/copy-static.mjs",
    "watch": "tsc --watch",
    "package": "npm run build && node scripts/package-zip.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "typescript": "^5.6.3"
  }
}
